import { FC } from 'react';
interface IFlashCardProps {}

export const FlashCard: FC<IFlashCardProps> = (props) => {
  return (
    <div>
      <h1>List</h1>
    </div>
  );
};

export default FlashCard;
